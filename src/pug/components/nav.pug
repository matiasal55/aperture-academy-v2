.menu-items 
    a(href=`${direccion}/index.html`) Home
if !footer
    .menu-items.cursos
        .cursos-seccion
            .cursos-titulo
                a(href=`${direccion}/cursos.html`) Cursos
            .cursos-items
                - let lista=["HTML","Java","Javascript","Docker","Go"];
                each curso in lista
                    div 
                        a(href=`${direccion}/cursos/${curso.toLowerCase()}.html`) #{curso}
.menu-items
    a(href=`${direccion}/calendario.html`) Calendario
.menu-items
    a(href=`${direccion}/giftcard.html`) Gift Card
.menu-items
    a(href=`${direccion}/inscripcion.html`) Inscripción
.menu-items
    a(href=`${direccion}/contacto.html`) Contacto
if menu
    .menu-items
        .buscador-responsive
                input(type="text" id="datoabuscar")
                button(type="submit") Buscar